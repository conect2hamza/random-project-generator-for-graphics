.DS_Store
Thumbs.db
node_modules/
*.log

# Demo build intermediates. demo/index.html is committed so the demo can be
# opened without running the build; everything here is regenerated by
# `node demo/build.js`.
demo/.parity.json
demo/.parity.err
demo/.shell.json
demo/.shots/
demo/artifact.html
